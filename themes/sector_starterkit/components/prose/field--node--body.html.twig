{{ include('sector_starterkit:prose', {
    utility_classes: [ ],
    container: '@container/prose',
    libraries: [ ]
} ) }}

{# Optional extra components included in prose sections #}
{% include 'sector_starterkit:callout' with { } only %}
{% include 'sector_starterkit:button' with { } only %}
{% include 'sector_starterkit:link' with { } only %}
