{{ include('sector:region', {
    wrapper: 'footer',
    label: 'Footer',
    attributes: attributes.setAttribute('role', 'banner'),
    libraries: ['sector_starter/' ~ region|clean_class ],
    container__classes: [ 'container' ]
}) }}