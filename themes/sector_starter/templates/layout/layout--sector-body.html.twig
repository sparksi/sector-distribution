{#
/**
 * @file
 * Template for a block.
 */
#}
{% set utility_classes = [
    'layout--body',
    content.body_prefix ? 'layout--has-prefix',
    content.body_suffix ? 'layout--has-suffix',
] %}

{% include '@sector_base/layout/body.twig' with {
  html_tag: 'div',
  utility_classes: utility_classes,
  libraries: [
    'sector_starter/layout-body',
  ]
} %}
