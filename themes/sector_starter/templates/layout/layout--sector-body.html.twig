{#
/**
 * @file
 * Template for a block.
 */
#}
{% set utility_classes = [
    'layout--body',
    content.body_prefix|render|striptags('<drupal-render-placeholder>')|trim is not empty ? 'layout--has-prefix',
    content.body_suffix|render|striptags('<drupal-render-placeholder>')|trim is not empty ? 'layout--has-suffix',
] %}

{% include '@sector_base/layout/body.twig' with {
  html_tag: 'div',
  utility_classes: utility_classes,
  libraries: [
    'sector_starter/layout',
    'sector_starter/layout-body',
  ]
} %}
