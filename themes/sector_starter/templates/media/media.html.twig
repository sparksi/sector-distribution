{#
/**
 * @file
 * Template for a general use media.
 */
#}
{% include "@sector_base/media/media.twig" with {
    utility_classes: [],
    libraries: [
        'sector_starter/media',
        'sector_starter/' ~ media.bundle()|replace({"_": "-"})|clean_class,
        'sector_starter/media-' ~ view_mode,
        'sector_starter/' ~ media.bundle()|replace({"_": "-"})|clean_class ~ '-' ~ view_mode
    ]
} %}
