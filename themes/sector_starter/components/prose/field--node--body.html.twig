{{ include('sector_starter:prose', {
    utility_classes: [ ],
    container: '@container/prose',
    libraries: [ ]
} ) }}

{# Optional extra components included in prose sections #}
{% include 'sector_starter:callout' with { } only %}
{% include 'sector_starter:button' with { } only %}
{% include 'sector_starter:link' with { } only %}