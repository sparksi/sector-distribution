{#
/**
 * @file
 * Default theme implementation to display source summary block total count.
 *
 * Available variables:
 * - count: The total number of records retrieved.
 */
#}
<span class="source-summary__count font-black tracking-tight text-2xl lg:text-3xl">
  {% trans %}<mark class="bg-transparent text-sky-500">1</mark> result found{% plural count %}<mark class="bg-transparent text-sky-500">{{ count }}</mark> results found{% endtrans %}
</span>
