{{ include('sector_demo:prose', {
    utility_classes: [ ],
    container: '@container/prose',
    libraries: [ ]
} ) }}

{# Optional extra components included in prose sections #}
{% include 'sector_demo:callout' with { } only %}
{% include 'sector_demo:button' with { } only %}
{% include 'sector_demo:link' with { } only %}