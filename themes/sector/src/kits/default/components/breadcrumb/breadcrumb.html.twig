{#
/**
 * @file
 * Template for the breadcrumb.
 */
#}
{{ include('SECTOR_SUBTHEME_MACHINE_NAME:breadcrumb', {
  breadcrumb: breadcrumb,
  utility_classes: [ 'breadcrumb' ],
  libraries: [
    'SECTOR_SUBTHEME_MACHINE_NAME/breadcrumb'
  ]
}) }}
