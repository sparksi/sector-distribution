{% include '@sector_base/region/region.twig' with {
    wrapper: 'footer',
    label: 'Footer',
    attributes: attributes.setAttribute('role', 'banner'),
    libraries: ['SECTOR_SUBTHEME_MACHINE_NAME/' ~ region|clean_class ],
    container__classes: [ 'container' ]
} %}