{#
/**
 * @file
 * Template for a region.
 */
#}
{% if content %}
  {{ content }}
{% endif %}
{% if libraries %}
  {% for library in libraries %}
    {{ attach_library(library) }}
  {% endfor %}
{% endif %}