<?php
use Drupal\system\Entity\Menu;

/*
 * Implements hook_uninstall()
 */
function sector_social_follow_uninstall() {
  $menu = Menu::load('sector-follow-menu');
  if($menu) {
    $menu->delete();
  }
}
