langcode: en
status: true
dependencies:
  config:
    - field.storage.media.field_poster
    - media.type.image
    - media.type.video
id: media.video.field_poster
field_name: field_poster
entity_type: media
bundle: video
label: Poster
description: "An image to be shown while the video is downloading. If this attribute isn't specified, nothing is displayed until the first frame is available, then the first frame is shown as the poster frame."
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:media'
  handler_settings:
    target_bundles:
      image: image
    sort:
      field: _none
      direction: ASC
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
